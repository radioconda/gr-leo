# auto-generated by grc.converter

id: variable_leo_model_def
label: LEO Model Definition
flags: [show_id]

parameters:
-   id: value
    label: Ignore Me
    dtype: raw
    default: '''fine'''
    hide: all
-   id: tracker
    label: Tracker
    dtype: raw
    default: tracker_variable
-   id: mode
    label: Mode
    dtype: int
    default: '0'
    options: ['0', '1']
    option_labels: [Uplink, Downlink]
-   id: fspl_attenuation_enum
    label: Free-space path Loss
    dtype: int
    default: '5'
    options: ['0', '5']
    option_labels: ['No', 'Yes']
-   id: doppler_shift_enum
    label: Doppler frequency shift
    dtype: int
    default: '7'
    options: ['0', '7']
    option_labels: ['No', 'Yes']
-   id: pointing_attenuation_enum
    label: Antenna pointing loss
    dtype: int
    default: '6'
    options: ['0', '6']
    option_labels: ['No', 'Yes']
-   id: atmo_gases_attenuation
    label: Atmospheric gases attenuation
    dtype: int
    default: '0'
    options: ['0', '1', '2']
    option_labels: [None, ITU Recommendation, Regression Analysis]
-   id: surface_watervap_density
    label: Surface H2O Density (g/m3)
    dtype: real
    default: 7.5
    hide: ${ ('none' if int(atmo_gases_attenuation)==2 else 'all') }
-   id: temperature
    label: Surface Temperature (Celsius)
    dtype: real
    default: 0
    hide: ${ ('none' if int(atmo_gases_attenuation)==2 else 'all') }
-   id: precipitation_attenuation
    label: Precipitation attenuation
    dtype: int
    default: '0'
    options: ['0', '3', '4']
    option_labels: [None, ITU Recommendation, Local Measurement]
-   id: rainfall_rate
    label: Rainfall Rate (mm3/h)
    dtype: real
    default: 25
    hide: ${ ('none' if int(precipitation_attenuation)==4 else 'all') }
-   id: enable_link_margin
    label: Link Margin Calculation
    dtype: bool
    default: true
    options: [true, false]
    option_labels: ['On', 'Off']
value: "${ gnuradio.leo.model.leo_model.make(tracker, mode, fspl_attenuation_enum, \n  \t\t\t\t\t\
    \t\t\tpointing_attenuation_enum, doppler_shift_enum, atmo_gases_attenuation, \n\
    \  \t\t\t\t\t\t\t\tprecipitation_attenuation, enable_link_margin, surface_watervap_density, temperature,\
    \ rainfall_rate) }"

templates:
    imports: import gnuradio.leo
    var_make: "self.${id} = ${id} = gnuradio.leo.model.leo_model.make(${tracker}, ${mode}, ${fspl_attenuation_enum},\
        \ \n  \t\t\t\t\t\t\t\t\t\t\t\t\t${pointing_attenuation_enum}, ${doppler_shift_enum},\n\
        \  \t\t\t\t\t\t\t\t\t\t\t\t\t${atmo_gases_attenuation}, ${precipitation_attenuation},\n\
        \  \t\t\t\t\t\t\t\t\t\t\t\t\t${enable_link_margin}, ${surface_watervap_density}, ${temperature},\
        \ ${rainfall_rate})"

file_format: 1
